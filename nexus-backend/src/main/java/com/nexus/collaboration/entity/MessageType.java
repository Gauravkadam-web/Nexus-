package com.nexus.collaboration.entity;

/**
 * Message types supported across the case collaboration timeline.
 */
public enum MessageType {
    QUESTION,
    ANSWER,
    UPDATE,
    EVIDENCE_REQUEST,
    RESOLUTION_MESSAGE,
    FOLLOW_UP
}
